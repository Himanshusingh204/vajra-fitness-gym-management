# Base URL of the backend API. In local dev it points at your local server.
# For production (Netlify), set this in the Netlify dashboard under
# Site settings > Environment variables, then trigger a new build.
VITE_API_URL="http://localhost:5000/api"
